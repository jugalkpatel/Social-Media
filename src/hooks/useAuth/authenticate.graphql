mutation Authenticate {
  authenticate {
    ... on AuthPayload {
      user {
        id
        name
        picture
        __typename
      }
    }
    ... on AuthError {
      message
      __typename
    }
  }
}
