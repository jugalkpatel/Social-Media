mutation Authenticate {
  authenticate {
    ... on AuthPayload {
      user {
        id
        name
        __typename
      }
    }
    ... on AuthError {
      message
      __typename
    }
  }
}
